import styled from "styled-components";

export default function NaverLoginButton() {
  return <NaverLoginButtonContainer></NaverLoginButtonContainer>;
}

const NaverLoginButtonContainer = styled.div`
  width: 48px;
  aspect-ratio: 1;
  border-radius: 100%;
  background-color: greenyellow;
  margin: 0 10px;
`;
