// components/account/login/client/LoginForm.css.ts
import { style } from "@vanilla-extract/css";

export const form = style({
  width: "100%",
  height: "100%",
});

export const buttonWrapper = style({
  margin: "20px 0",
});

export const textButtonWrapper = style({
  display: "flex",
  width: "100%",
  alignItems: "center",
  justifyContent: "center",
});
